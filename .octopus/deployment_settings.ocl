connectivity_policy {
    allow_deployments_to_no_targets = true
}

versioning_strategy {
    donor_package {
        package = "template-sbom-artifact"
        step = "verify-artifacts-attach-sbom-to-release"
    }
}